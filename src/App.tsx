import React from "react";

export default function App() {
  return (
    <div style={{ padding: 24 }}>
      <h1 style={{ marginBottom: 12 }}>Stratfit Godmode</h1>
      <p style={{ marginBottom: 16 }}>
        App shell is active. Use the Initialize Baseline flow to configure your starting parameters.
      </p>
      <a href="#/initialize-baseline" style={{ color: "#22d3ee" }}>
        Go to Initialize Baseline
      </a>
    </div>
  );
}
