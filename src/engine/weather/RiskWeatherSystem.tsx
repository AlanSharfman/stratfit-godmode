/**
 * RiskWeatherSystem — disabled (riskWeatherStore was removed).
 * Kept as a no-op export so existing imports compile.
 */
export default function RiskWeatherSystem(_props: { radius?: number; height?: number }) {
    return null;
}
