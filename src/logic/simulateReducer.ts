// src/logic/simulateReducer.ts
/**
 * @deprecated Legacy simulation reducer (dual-verdict system).
 *
 * Current architecture uses `src/logic/verdictGenerator.ts`.
 * This file is kept as a thin compatibility shim so legacy UI can compile
 * while the legacy implementation lives in `_archive/legacy/`.
 */

export * from "../../_archive/legacy/simulateReducer";


