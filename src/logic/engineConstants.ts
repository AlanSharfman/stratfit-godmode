// src/logic/engineConstants.ts
// Shared constants used across multiple engine modules (Sensitivity, Risk, SystemAnalysis).

/** Default enterprise-value-to-ARR multiple used across engines. */
export const DEFAULT_EV_MULTIPLE = 3.5;
