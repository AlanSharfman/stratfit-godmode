# Example environment variables for local dev (SANITIZED)
# Do NOT put real keys in this file.

# Optional: enables OpenAI-powered Q&A features in the UI.
# If unset/blank, the app falls back to deterministic/local answers.
VITE_OPENAI_API_KEY=

# Optional alternative (dev convenience). Not read from env; shown for reference:
# localStorage key: OPENAI_API_KEY
